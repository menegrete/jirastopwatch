<Window x:Class="StopWatch.EditTimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:ClassModifier="internal"
        Title="Edit Timer"
        Background="{DynamicResource Background}"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI"
        FontSize="12">

    <StackPanel Margin="16" Width="240">
        <TextBlock Text="Edit timer" FontWeight="SemiBold" />

        <TextBox x:Name="tbTime"
                 Margin="0,8,0,0"
                 TextAlignment="Right"
                 TextChanged="tbTime_TextChanged" />

        <TextBlock Margin="0,6,0,0"
                   Style="{StaticResource MutedText}"
                   TextWrapping="Wrap">
            Supported formats:<LineBreak />2h 15m and 2.25h
        </TextBlock>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="OK" IsDefault="True" Click="btnOk_Click" />
            <Button Content="Cancel" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
